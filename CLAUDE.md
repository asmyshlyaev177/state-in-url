# CLAUDE.md

The project guide lives in **[AGENTS.md](AGENTS.md)** — the cross-tool
convention, so Codex, Cursor, Copilot and the rest read the same file instead of
each getting its own drifting copy. Everything that used to be here (commands,
layout, the architecture walk-through, the invariants that bite, Lighthouse, and
the commit conventions) is there.

@AGENTS.md
